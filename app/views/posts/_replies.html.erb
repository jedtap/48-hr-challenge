<% @replies.where(comment_id: comment_id).each do |reply| %>
    <br/>
    <%= reply.feedback %>
    <br/>
    <div>


      <% if current_user.id == reply.user_id %>
                You
            <% else %>
                <%= User.where(id: reply.user_id).first.name %>
            <% end %>

          replied 
            <%= time_ago_in_words(Reply.where(id: reply.id).last.updated_at) %>
          
          ago • 


        
        
            <span>        
                <%= form_with(model: reply, method: :delete) do |form| %>
                    <%= form.submit "Delete" %>
                <% end %>
            </span>
        
        
         • <span data-action="click->replies#toggle"> Reply </span>
    </div>

<% end %>
